Enthält die Doxygen-Konfiguration zum Erstellen der Quellcode-Docu.

Mit `make' oder `make doc' wird die HTML-Doku erstellt,
mit `make qtdoc' das Help-File, was in den Qt-Creator importiert werden kann,
mit `make doc-gnav' die HTML-Doku, allerdings wird hier die JS-Datei mitgeladen,
die die globale Navigationsleiste aufbaut.
