
#include <math.h>

// fuck this eCos, we have our own

float sinf(float x) {
  return sin(x);
}

float cosf(float x) {
  return cos(x);
}

float atan2f(float y, float x) {
  return atan2(y, x);
}

float sqrtf(float x) {
  return sqrtf(x);
}
